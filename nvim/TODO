- get anything I need for work on mise, figure out if I can access poetry lsps in nvim (how should I install pyright?)
- see if I can get rid of direnv?
- more lsps (pyright, go, json + schemastore?)
- lsp config (extra keybinds, hover, diagnostics config) (checkout kickstart)
- blink config
- format on save (conform?)
- autopairs
- colorscheme + styling nitpicks
