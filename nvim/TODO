- more lsps ([based]pyright, go, json + schemastore?)
- lsp config (extra keybinds, hover, diagnostics config) (checkout kickstart)
- blink config
- format on save (conform?)
- autopairs
- colorscheme + styling nitpicks
- winbar (filename + modified status)

- reinstall other dev tools through mise if I can (colima, docker, terraform, etc)
